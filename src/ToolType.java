/**
 * @author Trinh Nguyen
 * Used to differentiate between what tools are being used to draw shapes so that
 * the appropriate shape is created.
 */
public enum ToolType {
	RECT, OVAL, LINE, TEXT, SELECT
}
